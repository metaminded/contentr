require 'spec_helper'

describe Contentr::PageType do
  describe '#valid?' do
    # it 'assures that the combination of col offset and width is 20' do
    #   page_type = build(:page_type, col1_width: 4, col2_width: 10, col3_width: 2)
    #   expect(page_type).to_not be_valid
    #   expect(page_type.errors.messages[:base].join('')).to match(/width \+ offset should be/)
    # end
  end
end
