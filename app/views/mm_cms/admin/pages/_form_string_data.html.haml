- if data.type == 'string'
  = form.input :name, :value => data.name, :as => :hidden
  = form.input :value, :label => data_description.label, :input_html => {:value => data.try(:value)}, :hint => data_description.description

