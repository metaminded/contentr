- contentr_title @content_block.name
- contentr_navigation do |navbar|
  - navbar.divider
  - @content_block.usages.each do |usage|
    - navbar.link usage.page.name, contentr.admin_page_path(usage.page)

= render 'form'
